

/* Things to evaluate
 *
 *
 * Arithmetic * /  + -
 * Logic      == < > != || && !
 * Control flow APPLY IF ELSE WHILE CONTINUE BREAK RETURN
 */

switch(op->type) {
 case '+': case '-': case '*': case '/':
  break;
 case APPLY:
  break;
 case IF:
  break;
 case ELSE:
  break;
 case RETURN:
  break;
 case BREAK:
  break;
 default:
  return;
}
